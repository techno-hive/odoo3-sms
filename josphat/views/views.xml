<odoo>
  <data>
    <!-- explicit list view definition -->
    <!--
    <record model="ir.ui.view" id="josphat.list">
      <field name="name">josphat list</field>
      <field name="model">josphat.josphat</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
    -->

    <!-- actions opening views on models -->
    <!--
    <record model="ir.actions.act_window" id="josphat.action_window">
      <field name="name">josphat window</field>
      <field name="res_model">josphat.josphat</field>
      <field name="view_mode">tree,form</field>
    </record>
    -->

    <!-- server action to the one above -->
    <!--
    <record model="ir.actions.server" id="josphat.action_server">
      <field name="name">josphat server</field>
      <field name="model_id" ref="model_josphat_josphat"/>
      <field name="state">code</field>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": self._name,
        }
      </field>
    </record>
    -->

    <!-- Top menu item -->
    <!--
    <menuitem name="josphat" id="josphat.menu_root"/>
    -->
    <!-- menu categories -->
    <!--
    <menuitem name="Menu 1" id="josphat.menu_1" parent="josphat.menu_root"/>
    <menuitem name="Menu 2" id="josphat.menu_2" parent="josphat.menu_root"/>
    -->
    <!-- actions -->
    <!--
    <menuitem name="List" id="josphat.menu_1_list" parent="josphat.menu_1"
              action="josphat.action_window"/>
    <menuitem name="Server to list" id="josphat" parent="josphat.menu_2"
              action="josphat.action_server"/>
    -->
  </data>
</odoo>